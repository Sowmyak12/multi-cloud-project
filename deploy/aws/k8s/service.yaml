apiVersion: v1
kind: Service
metadata:
  name: taskflow-api
  labels:
    app: taskflow-api
spec:
  type: LoadBalancer
  selector:
    app: taskflow-api
  ports:
    - port: 80
      targetPort: 8000
      name: http
