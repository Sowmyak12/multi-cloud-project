Screenshots referenced by the main [README.md](../../README.md):

**GCP (GitOps/ArgoCD):**
- `argocd-apps.png` — ArgoCD UI showing the 4 synced Applications
- `grafana-dashboard.png` — the "TaskFlow — SLO & Deploy Frequency" dashboard with real traffic
- `api-response.png` — a terminal `curl` against the live GCP API

**AWS (Jenkins):**
- `jenkins-pipeline.png` — the `taskflow-aws-deploy` job showing a successful build
- `jenkins-deploy-log.png` — console output of the deploy stage rolling out to EKS
- `aws-api-response.png` — a terminal `curl` against the live AWS API
